# Insider Sentinel

Minimal runnable website scaffold for Insider Sentinel.

## Run locally

1. Create and activate a virtual environment (optional):
   - `python -m venv .venv`
   - `source .venv/bin/activate` (Linux/macOS) or `.venv\\Scripts\\activate` (Windows)
2. Install dependencies:
   - `pip install -r requirements.txt`
3. Start the app:
   - `python app.py`
4. Open:
   - `http://127.0.0.1:5000`

## Demo login

- Username: `admin`
- Password: `admin123`
